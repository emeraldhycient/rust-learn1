// rust primitive types

// floats (f32,f64)
// boolean (bool)
// characters (char)
// tuples
// arrays
// integers (u8,i8,u16,i16,u32,i32,u64,i64,u128,i128) == number of bits dy occupy in memory

pub fn init() {
    let floats: f32 = 2.56;
    let boolean: bool = true;
    let characters: char = 'c';
    let integers: i64 = 12345566;

    // get boolean from expression
    let is_greater = 34 > 4;
}
